// for de-bugging ResultList, simple view of search results

const SimpleResult = (props) => {
    return (
        <div>
            <p>{props.results["5"]}</p>
        </div>
    )
}

export default SimpleResult;